#include<stdio.h>
int main()
{       float side,area;
	printf("Ender the length of side:");
	scanf("%f",&side);
	area=side*side;
	printf("The area of a squre whose side length is %f cm is %f square cm.\n",side,area);


	return 0;
}

